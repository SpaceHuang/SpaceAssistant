# i18n 第四期：收尾与验证 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成 i18n 国际化最后 7 项遗留任务——`formatChatTimestamp` 国际化、`browserSetupGuideContent` 工厂化、Electron 原生菜单、英文校对、strict CI 与文档。

**Architecture:** 分 A→B→C 三个子阶段。A 处理渲染进程遗留代码改造（`formatChatTimestamp`、`browserSetupGuideContent`），B 处理 Electron 主进程菜单（`menuLabels.ts` + `menu.ts` 重构），C 处理工程收尾（校对、CI、文档）。

**Tech Stack:** TypeScript, React, i18next, Electron, Vitest, Testing Library

**基线:** `a5a3b8b`，全量 913/913 测试通过，8 命名空间已落地

---

## 文件结构

| 文件 | 动作 | 职责 |
|------|------|------|
| `src/renderer/components/Chat/formatChatTimestamp.ts` | 修改 | 用 `i18next.language` 替代 `undefined` |
| `src/renderer/components/Chat/formatChatTimestamp.test.ts` | 修改 | 增强双语格式断言 |
| `src/shared/browserSetupGuideContent.ts` | 修改 | 工厂化：新增 `t` 参数 |
| `src/shared/browserSetupGuideContent.test.ts` | 修改 | 适配新签名 + 双语断言 |
| `src/renderer/i18n/resources/zh-CN/feishu.json` | 修改 | 新增 `browser.setup.*` 分支 |
| `src/renderer/i18n/resources/en-US/feishu.json` | 修改 | 新增 `browser.setup.*` 分支（英文） |
| `src/renderer/i18n/resources/zh-CN/common.json` | 修改 | 新增 `status.installed/notInstalled/ready` 通用状态 |
| `src/renderer/i18n/resources/en-US/common.json` | 修改 | 新增通用状态英文 |
| `src/renderer/components/Browser/BrowserSetupGuide.tsx` | 修改 | 接入 i18n，替换 ~20 处硬编码 |
| `src/renderer/components/Browser/BrowserSetupGuide.test.tsx` | 修改 | 适配 i18n 上下文，更新断言 |
| `src/shared/menuLabels.ts` | 新建 | 中英文菜单标签映射 |
| `electron/menu.ts` | 修改 | 接受 locale 参数，动态菜单标签 |
| `electron/main.ts` | 修改 | 传入 locale 给 `setupAppMenu` |
| `electron/appIpc.ts` | 修改 | `config:set` locale 变更时重建菜单 |
| `scripts/i18n-check.ts` | 修改 | 增加分类统计（test fixture vs 真正遗漏） |
| `package.json` | 修改 | 新增 `i18n:check:strict` script |
| `CLAUDE.md` | 修改 | 补充 i18n 开发规范 |
| `.github/workflows/` | 新建/修改 | CI 接入 i18n check + test |

---

### Task 1: `formatChatTimestamp` 国际化

**Files:**
- Modify: `src/renderer/components/Chat/formatChatTimestamp.ts`
- Modify: `src/renderer/components/Chat/formatChatTimestamp.test.ts`

- [ ] **Step 1: 增强测试——先写双语格式断言**

在 `src/renderer/components/Chat/formatChatTimestamp.test.ts` 中替换现有测试：

```typescript
import { describe, expect, it, vi, beforeEach, afterEach } from 'vitest'

// mock i18next.language 以便测试不同 locale
vi.mock('i18next', () => ({
  default: { language: 'zh-CN' }
}))

import { formatChatTimestamp } from './formatChatTimestamp'

describe('formatChatTimestamp', () => {
  beforeEach(() => {
    vi.useFakeTimers()
    // 固定"现在"为 2026-06-03 14:30:00 北京时间
    vi.setSystemTime(new Date('2026-06-03T06:30:00Z'))
  })

  afterEach(() => {
    vi.useRealTimers()
  })

  describe('zh-CN locale', () => {
    it('shows time only for today', async () => {
      const { default: i18n } = await vi.importActual('i18next')
      // 重新 mock 为 zh-CN
      vi.doMock('i18next', () => ({ default: { language: 'zh-CN' } }))
      const today = new Date('2026-06-03T02:00:00Z').getTime() // 10:00 北京时间
      const result = formatChatTimestamp(today)
      // 中文格式不含 AM/PM
      expect(result).not.toMatch(/AM|PM/i)
      expect(result).toMatch(/\d/)
    })

    it('shows date and time for older messages', () => {
      const old = new Date('2026-05-15T10:30:00Z').getTime()
      const result = formatChatTimestamp(old)
      expect(result).toMatch(/\//)
      expect(result).not.toMatch(/AM|PM/i)
    })
  })

  describe('en-US locale', () => {
    it('shows time with AM/PM for today', () => {
      vi.doMock('i18next', () => ({ default: { language: 'en-US' } }))
      // 由于 vitest 的 hoisting，mock 需要在 import 之前生效
      // 这里验证 en-US 下输出含 AM/PM 的行为
    })
  })
})
```

由于 vitest 的 `vi.mock` hoisting 与动态 locale 切换存在兼容性问题，实际采用**直接测试 `toLocaleString` 的 locale 参数**方案：

```typescript
import { describe, expect, it, vi } from 'vitest'

// Mock i18next 为固定 locale
const mockLanguage = { current: 'zh-CN' }
vi.mock('i18next', () => ({
  default: {
    get language() { return mockLanguage.current }
  }
}))

import { formatChatTimestamp } from './formatChatTimestamp'

describe('formatChatTimestamp', () => {
  beforeEach(() => {
    vi.useFakeTimers()
    vi.setSystemTime(new Date('2026-06-03T06:30:00Z')) // 14:30 北京时间
  })

  afterEach(() => {
    vi.useRealTimers()
  })

  describe('zh-CN locale', () => {
    it('shows time without AM/PM for today', () => {
      mockLanguage.current = 'zh-CN'
      const today = new Date('2026-06-03T02:00:00Z').getTime()
      const result = formatChatTimestamp(today)
      expect(result).not.toMatch(/AM|PM/i)
      expect(result).toMatch(/\d/)
    })

    it('shows date with slash for older messages', () => {
      mockLanguage.current = 'zh-CN'
      const old = new Date('2026-05-15T10:30:00Z').getTime()
      const result = formatChatTimestamp(old)
      expect(result).toMatch(/\//)
      expect(result).not.toMatch(/AM|PM/i)
    })
  })

  describe('en-US locale', () => {
    it('shows time with AM/PM for today', () => {
      mockLanguage.current = 'en-US'
      const today = new Date('2026-06-03T02:00:00Z').getTime()
      const result = formatChatTimestamp(today)
      expect(result).toMatch(/AM|PM/i)
    })

    it('shows date with slash for older messages', () => {
      mockLanguage.current = 'en-US'
      const old = new Date('2026-05-15T10:30:00Z').getTime()
      const result = formatChatTimestamp(old)
      expect(result).toMatch(/\//)
    })
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

```bash
npx vitest run src/renderer/components/Chat/formatChatTimestamp.test.ts
```

预期：zh-CN 测试通过（当前行为），en-US 测试失败（因为当前 `undefined` 跟随系统 locale，不一定输出 AM/PM）。

- [ ] **Step 3: 改造 `formatChatTimestamp.ts`**

```typescript
import i18next from 'i18next'

/** 聊天消息时间：当天仅显示时分，跨天显示月日+时分 */
export function formatChatTimestamp(timestamp: number): string {
  const date = new Date(timestamp)
  const now = new Date()
  const sameDay = date.toDateString() === now.toDateString()
  const opts: Intl.DateTimeFormatOptions = sameDay
    ? { hour: '2-digit', minute: '2-digit' }
    : { month: 'numeric', day: 'numeric', hour: '2-digit', minute: '2-digit' }
  return date.toLocaleString(i18next.language, opts)
}
```

- [ ] **Step 4: 运行测试确认通过**

```bash
npx vitest run src/renderer/components/Chat/formatChatTimestamp.test.ts
```

预期：4 个测试全部 PASS。

- [ ] **Step 5: 确保全量测试不受影响**

```bash
npx vitest run src/renderer/components/Chat/ChatBubble.test.ts
```

预期：PASS（`ChatBubble` 使用 `formatChatTimestamp`，行为应一致）。

- [ ] **Step 6: Commit**

```bash
git add src/renderer/components/Chat/formatChatTimestamp.ts src/renderer/components/Chat/formatChatTimestamp.test.ts
git commit -m "feat(i18n): formatChatTimestamp follows i18next.language instead of system locale"
```

---

### Task 2: 新增翻译 key——`browser.setup` 与通用状态

**Files:**
- Modify: `src/renderer/i18n/resources/zh-CN/feishu.json`
- Modify: `src/renderer/i18n/resources/en-US/feishu.json`
- Modify: `src/renderer/i18n/resources/zh-CN/common.json`
- Modify: `src/renderer/i18n/resources/en-US/common.json`

- [ ] **Step 1: 在 `zh-CN/feishu.json` 中追加 `browser.setup` 分支**

在文件末尾 `"auditAria"` 行之前，`"remote"` 对象之后追加：

```json
  "browser": {
    "setup": {
      "okTitle": "浏览器依赖已就绪",
      "fixTitle": "浏览器依赖修复",
      "okSummary": "Chromium 已就绪，可以使用浏览器工具。",
      "depNotReady": "浏览器依赖未就绪",
      "nodeTooLow": "应用内置 Node 版本过低，请升级 SpaceAssistant。",
      "terminalHintWin": "打开 Windows Terminal 或 PowerShell",
      "terminalHintMac": "打开「终端.app」",
      "terminalHintLinux": "打开终端",
      "cwdDescription": "请在下方目录打开终端（应用安装位置）",
      "stepInstall": "安装 npm 依赖后继续执行 Chromium 步骤：",
      "stepRunInstall": "执行安装命令：",
      "stepRedetect": "完成后点击「重新检测」",
      "troubleshootNetworkTitle": "网络",
      "troubleshootNetworkBody": "安装需联网下载约 150–200MB。若使用代理，请确保终端可访问 cdn.playwright.dev。",
      "troubleshootDiskTitle": "磁盘空间",
      "troubleshootDiskBody": "请至少预留 500MB 可用空间。",
      "troubleshootStillFailTitle": "仍失败",
      "troubleshootStillFailBody": "可点击「复制诊断信息」并将内容用于排查（不含 API Key）。",
      "troubleshootDefenderTitle": "Windows 杀毒",
      "troubleshootDefenderBody": "Windows Defender 可能隔离 %LOCALAPPDATA%\\ms-playwright 下的 chrome.exe，请添加排除项或允许运行后重试。",
      "troubleshootGatekeeperTitle": "macOS Gatekeeper",
      "troubleshootGatekeeperBody": "首次运行 Chromium 可能提示无法验证开发者。请到「系统设置 → 隐私与安全性」允许，或参考 Playwright 文档移除隔离属性。",
      "troubleshootCacheMacTitle": "缓存位置",
      "troubleshootCacheMacBody": "Playwright 默认将浏览器下载到 ~/Library/Caches/ms-playwright/",
      "troubleshootCacheLinuxTitle": "缓存位置",
      "troubleshootCacheLinuxBody": "Playwright 默认将浏览器下载到 ~/.cache/ms-playwright/"
    }
  }
```

- [ ] **Step 2: 在 `en-US/feishu.json` 中追加对应英文**

```json
  "browser": {
    "setup": {
      "okTitle": "Browser dependencies ready",
      "fixTitle": "Browser dependency repair",
      "okSummary": "Chromium is ready. Browser tools are available.",
      "depNotReady": "Browser dependencies not ready",
      "nodeTooLow": "Built-in Node version is too old. Please upgrade SpaceAssistant.",
      "terminalHintWin": "Open Windows Terminal or PowerShell",
      "terminalHintMac": "Open Terminal.app",
      "terminalHintLinux": "Open a terminal",
      "cwdDescription": "Open a terminal in this directory (the app install location)",
      "stepInstall": "Install npm dependencies then continue with the Chromium step:",
      "stepRunInstall": "Run the install command:",
      "stepRedetect": "Click \"Re-detect\" when done",
      "troubleshootNetworkTitle": "Network",
      "troubleshootNetworkBody": "Installation downloads ~150–200MB. If using a proxy, make sure the terminal can reach cdn.playwright.dev.",
      "troubleshootDiskTitle": "Disk space",
      "troubleshootDiskBody": "Please ensure at least 500MB of free space.",
      "troubleshootStillFailTitle": "Still failing",
      "troubleshootStillFailBody": "Click \"Copy diagnostic info\" and share the output for troubleshooting (no API Key included).",
      "troubleshootDefenderTitle": "Windows Defender",
      "troubleshootDefenderBody": "Windows Defender may quarantine chrome.exe under %LOCALAPPDATA%\\ms-playwright. Add an exclusion or allow it to run, then retry.",
      "troubleshootGatekeeperTitle": "macOS Gatekeeper",
      "troubleshootGatekeeperBody": "The first Chromium launch may be blocked. Go to System Settings → Privacy & Security to allow it, or follow the Playwright docs to remove the quarantine attribute.",
      "troubleshootCacheMacTitle": "Cache location",
      "troubleshootCacheMacBody": "Playwright downloads browsers to ~/Library/Caches/ms-playwright/ by default.",
      "troubleshootCacheLinuxTitle": "Cache location",
      "troubleshootCacheLinuxBody": "Playwright downloads browsers to ~/.cache/ms-playwright/ by default."
    }
  }
```

- [ ] **Step 3: 在 `zh-CN/common.json` 和 `en-US/common.json` 中追加通用状态**

在 `zh-CN/common.json` 中追加（在 `"unsavedChanges"` 之前）：

```json
  "status": {
    "installed": "已安装",
    "notInstalled": "未安装",
    "ready": "已就绪",
    "builtinNode": "（应用内置）"
  }
```

在 `en-US/common.json` 中追加：

```json
  "status": {
    "installed": "Installed",
    "notInstalled": "Not installed",
    "ready": "Ready",
    "builtinNode": "(built-in)"
  }
```

- [ ] **Step 4: 重新生成类型**

```bash
npm run i18n:generate-types
```

- [ ] **Step 5: 验证 key 对齐**

```bash
npm run i18n:check
```

预期：✅ i18n check passed（key 对齐，JSON 合法）。

- [ ] **Step 6: Commit**

```bash
git add src/renderer/i18n/resources/zh-CN/feishu.json src/renderer/i18n/resources/en-US/feishu.json src/renderer/i18n/resources/zh-CN/common.json src/renderer/i18n/resources/en-US/common.json src/renderer/i18n/types.ts
git commit -m "feat(i18n): add browser.setup and common.status translation keys for BrowserSetupGuide"
```

---

### Task 3: `browserSetupGuideContent.ts` 工厂化

**Files:**
- Modify: `src/shared/browserSetupGuideContent.ts`
- Modify: `src/shared/browserSetupGuideContent.test.ts`

- [ ] **Step 1: 改造 `buildBrowserSetupGuideContent` 签名与实现**

在 `src/shared/browserSetupGuideContent.ts` 中，将 `buildBrowserSetupGuideContent` 改为接收第三个参数 `t`：

```typescript
import type { BrowserDetectResult } from './browserTypes'
import { CHROMIUM_INSTALL_CMD, NPM_INSTALL_CMD } from './browserTypes'

export type BrowserSetupGuideContent = {
  title: string
  summary: string
  terminalHint: string
  cwdLabel: string
  showNpmInstall: boolean
  npmInstallCmd: string
  chromiumInstallCmd: string
  showForceInstall: boolean
  forceInstallCmd: string
  troubleshooting: Array<{ title: string; body: string }>
  showPackagedDefect: boolean
}

const FORCE_INSTALL_CMD = 'npx playwright install --force chromium'

function terminalHint(platform: string, t: (key: string) => string): string {
  if (platform === 'win32') return t('feishu:browser.setup.terminalHintWin')
  if (platform === 'darwin') return t('feishu:browser.setup.terminalHintMac')
  return t('feishu:browser.setup.terminalHintLinux')
}

function cwdDescription(_detect: BrowserDetectResult, t: (key: string) => string): string {
  return t('feishu:browser.setup.cwdDescription')
}

function buildTroubleshooting(
  platform: string,
  t: (key: string) => string
): Array<{ title: string; body: string }> {
  const common = [
    {
      title: t('feishu:browser.setup.troubleshootNetworkTitle'),
      body: t('feishu:browser.setup.troubleshootNetworkBody')
    },
    {
      title: t('feishu:browser.setup.troubleshootDiskTitle'),
      body: t('feishu:browser.setup.troubleshootDiskBody')
    },
    {
      title: t('feishu:browser.setup.troubleshootStillFailTitle'),
      body: t('feishu:browser.setup.troubleshootStillFailBody')
    }
  ]

  if (platform === 'win32') {
    return [
      {
        title: t('feishu:browser.setup.troubleshootDefenderTitle'),
        body: t('feishu:browser.setup.troubleshootDefenderBody')
      },
      ...common
    ]
  }

  if (platform === 'darwin') {
    return [
      {
        title: t('feishu:browser.setup.troubleshootGatekeeperTitle'),
        body: t('feishu:browser.setup.troubleshootGatekeeperBody')
      },
      {
        title: t('feishu:browser.setup.troubleshootCacheMacTitle'),
        body: t('feishu:browser.setup.troubleshootCacheMacBody')
      },
      ...common
    ]
  }

  return [
    {
      title: t('feishu:browser.setup.troubleshootCacheLinuxTitle'),
      body: t('feishu:browser.setup.troubleshootCacheLinuxBody')
    },
    ...common
  ]
}

export function buildBrowserSetupGuideContent(
  detect: BrowserDetectResult,
  platform: string,
  t: (key: string, options?: Record<string, string>) => string
): BrowserSetupGuideContent {
  const failure = detect.primaryFailure
  const showNpmInstall = false
  const showPackagedDefect = failure === 'stagehand_missing' || failure === 'playwright_missing'
  const showForceInstall =
    failure === 'chromium_missing' ||
    failure === 'chromium_headless_only' ||
    failure === 'chromium_path_unresolved' ||
    failure === 'init_probe_failed'

  let summary = detect.errors[0] ?? t('feishu:browser.setup.depNotReady')
  if (failure === 'ok') {
    summary = t('feishu:browser.setup.okSummary')
  } else if (failure === 'node_version_low') {
    summary = detect.errors[0] ?? t('feishu:browser.setup.nodeTooLow')
  }

  return {
    title: failure === 'ok' ? t('feishu:browser.setup.okTitle') : t('feishu:browser.setup.fixTitle'),
    summary,
    terminalHint: terminalHint(platform, t),
    cwdLabel: cwdDescription(detect, t),
    showNpmInstall,
    npmInstallCmd: NPM_INSTALL_CMD,
    chromiumInstallCmd: CHROMIUM_INSTALL_CMD,
    showForceInstall,
    forceInstallCmd: FORCE_INSTALL_CMD,
    troubleshooting: buildTroubleshooting(platform, t),
    showPackagedDefect
  }
}

// buildDiagnosticText 保持不变——纯技术诊断信息，不需要翻译
export function buildDiagnosticText(detect: BrowserDetectResult, platform: string): string {
  return [
    `failureCode: ${detect.primaryFailure}`,
    `platform: ${platform}`,
    `installContext: ${detect.installContext}`,
    `stagehand: ${detect.stagehand.installed ? detect.stagehand.version ?? 'installed' : 'missing'}`,
    `playwright: ${detect.playwright.installed ? 'installed' : 'missing'}`,
    `chromiumReady: ${detect.chromium.ready}`,
    `node: ${detect.node.version}`
  ].join('\n')
}
```

- [ ] **Step 2: 改造测试——适配新签名 + 双语断言**

替换 `src/shared/browserSetupGuideContent.test.ts`：

```typescript
import { describe, expect, it } from 'vitest'
import { buildBrowserSetupGuideContent } from './browserSetupGuideContent'
import type { BrowserDetectResult } from './browserTypes'

function detect(partial: Partial<BrowserDetectResult>): BrowserDetectResult {
  return {
    stagehand: { installed: true, version: '3.0.0' },
    playwright: { installed: true, browsers: ['chromium'] },
    chromium: { ready: false },
    node: { version: 'v22.0.0', meetsRequirement: true },
    canInitialize: false,
    primaryFailure: 'chromium_missing',
    errors: ['Chromium 未安装'],
    recommendedCwd: 'E:\\Develop\\SpaceAssistant',
    installContext: 'development',
    ...partial
  }
}

// mock t: 返回 key 本身即可验证结构
const t = (key: string): string => key

describe('browserSetupGuideContent', () => {
  it('does not show npm install when only chromium missing', () => {
    const content = buildBrowserSetupGuideContent(detect({ primaryFailure: 'chromium_missing' }), 'win32', t)
    expect(content.showNpmInstall).toBe(false)
    expect(content.chromiumInstallCmd).toContain('playwright install chromium')
  })

  it('returns translated title via t function', () => {
    const content = buildBrowserSetupGuideContent(detect({ primaryFailure: 'chromium_missing' }), 'win32', t)
    expect(content.title).toBe('feishu:browser.setup.fixTitle')
  })

  it('ok failure returns ok title', () => {
    const content = buildBrowserSetupGuideContent(
      detect({ primaryFailure: 'ok', errors: [] }),
      'win32',
      t
    )
    expect(content.title).toBe('feishu:browser.setup.okTitle')
    expect(content.summary).toBe('feishu:browser.setup.okSummary')
  })

  it('node_version_low returns nodeTooLow summary when no errors', () => {
    const content = buildBrowserSetupGuideContent(
      detect({ primaryFailure: 'node_version_low', errors: [] }),
      'win32',
      t
    )
    expect(content.summary).toBe('feishu:browser.setup.nodeTooLow')
  })

  it('L1 missing shows packaged defect not npm install', () => {
    const content = buildBrowserSetupGuideContent(
      detect({ primaryFailure: 'playwright_missing', playwright: { installed: false, browsers: [] } }),
      'win32',
      t
    )
    expect(content.showNpmInstall).toBe(false)
    expect(content.showPackagedDefect).toBe(true)
  })

  it('stagehand missing shows packaged defect', () => {
    const content = buildBrowserSetupGuideContent(
      detect({
        primaryFailure: 'stagehand_missing',
        stagehand: { installed: false },
        installContext: 'packaged'
      }),
      'darwin',
      t
    )
    expect(content.showNpmInstall).toBe(false)
    expect(content.showPackagedDefect).toBe(true)
  })

  it('windows troubleshooting includes defender key', () => {
    const content = buildBrowserSetupGuideContent(detect({}), 'win32', t)
    expect(content.troubleshooting.some((item) => item.title.includes('Defender'))).toBe(true)
  })

  it('mac troubleshooting includes gatekeeper key', () => {
    const content = buildBrowserSetupGuideContent(detect({}), 'darwin', t)
    expect(content.troubleshooting.some((item) => item.title.includes('Gatekeeper'))).toBe(true)
  })

  it('linux troubleshooting includes cache key', () => {
    const content = buildBrowserSetupGuideContent(detect({}), 'linux', t)
    expect(content.troubleshooting.some((item) => item.title.includes('Cache'))).toBe(true)
  })

  it('terminal hint uses platform-specific key', () => {
    const win = buildBrowserSetupGuideContent(detect({}), 'win32', t)
    const mac = buildBrowserSetupGuideContent(detect({}), 'darwin', t)
    const linux = buildBrowserSetupGuideContent(detect({}), 'linux', t)
    expect(win.terminalHint).toBe('feishu:browser.setup.terminalHintWin')
    expect(mac.terminalHint).toBe('feishu:browser.setup.terminalHintMac')
    expect(linux.terminalHint).toBe('feishu:browser.setup.terminalHintLinux')
  })
})
```

- [ ] **Step 3: 运行测试确认通过**

```bash
npx vitest run src/shared/browserSetupGuideContent.test.ts
```

预期：9 个测试 PASS。

- [ ] **Step 4: Commit**

```bash
git add src/shared/browserSetupGuideContent.ts src/shared/browserSetupGuideContent.test.ts
git commit -m "refactor(i18n): make buildBrowserSetupGuideContent accept translation function"
```

---

### Task 4: `BrowserSetupGuide.tsx` 组件 i18n 迁移

**Files:**
- Modify: `src/renderer/components/Browser/BrowserSetupGuide.tsx`
- Modify: `src/renderer/components/Browser/BrowserSetupGuide.test.tsx`

- [ ] **Step 1: 改造 `BrowserSetupGuide.tsx`——接入 i18n**

替换文件中所有硬编码中文。关键改动：

```typescript
import { useCallback, useEffect, useRef, useState } from 'react'
import { Alert, App, Button, Collapse, Space, Typography } from 'antd'
import type { BrowserDetectResult } from '../../../shared/browserTypes'
import {
  buildBrowserSetupGuideContent,
  buildDiagnosticText
} from '../../../shared/browserSetupGuideContent'
import { formatUserFacingError } from '../../utils/formatUserFacingError'
import { useTypedTranslation } from '../../i18n/useTypedTranslation'

type Props = {
  detect: BrowserDetectResult | null
  detecting?: boolean
  onRefresh: (force?: boolean) => Promise<void>
  mode?: 'settings' | 'chat'
  platform?: string
}

function detectPlatform(): string {
  if (typeof navigator !== 'undefined' && /Mac/i.test(navigator.userAgent)) return 'darwin'
  if (typeof navigator !== 'undefined' && /Win/i.test(navigator.userAgent)) return 'win32'
  return 'linux'
}

function isBrowserEnvironmentReady(detect: BrowserDetectResult): boolean {
  return (
    detect.canInitialize &&
    detect.stagehand.installed &&
    detect.playwright.installed &&
    detect.chromium.ready &&
    detect.node.meetsRequirement
  )
}

function ExpandChevronIcon() {
  return (
    <svg
      className="browser-setup-guide__expand-icon"
      xmlns="http://www.w3.org/2000/svg"
      width="14"
      height="14"
      viewBox="0 0 24 24"
      aria-hidden
    >
      <path fill="currentColor" d="M8.59 16.59 13.17 12 8.59 7.41 10 6l6 6-6 6z" />
    </svg>
  )
}

export function BrowserSetupGuide({
  detect,
  detecting = false,
  onRefresh,
  mode = 'settings',
  platform = detectPlatform()
}: Props) {
  const { message } = App.useApp()
  const { t } = useTypedTranslation('common')
  const { t: tf } = useTypedTranslation('feishu')
  const [detailsExpanded, setDetailsExpanded] = useState(false)
  const wasReadyRef = useRef<boolean | null>(null)

  const copyText = useCallback(
    async (text: string, label: string) => {
      await navigator.clipboard.writeText(text)
      message.success(`${label} ${t('common:status.installed')}`)
    },
    [message, t]
  )

  useEffect(() => {
    if (!detect) return
    const ready = isBrowserEnvironmentReady(detect)
    if (!ready) {
      setDetailsExpanded(true)
    } else if (wasReadyRef.current === false) {
      setDetailsExpanded(false)
    }
    wasReadyRef.current = ready
  }, [detect])

  if (!detect) return null

  // 传入 feishu namespace 的 t 函数给 buildBrowserSetupGuideContent
  const content = buildBrowserSetupGuideContent(detect, platform, (key: string) => tf(key as Parameters<typeof tf>[0]))
  const ready = isBrowserEnvironmentReady(detect)
  const guideClass =
    mode === 'chat' ? 'browser-setup-guide browser-setup-guide--chat' : 'browser-setup-guide'

  if (ready && !detailsExpanded) {
    const expand = () => setDetailsExpanded(true)
    return (
      <div
        className="browser-setup-guide--compact-wrap"
        title={t('feishu:browser.setup.clickToExpand')}
        role="button"
        tabIndex={0}
        onClick={expand}
        onKeyDown={(e) => {
          if (e.key === 'Enter' || e.key === ' ') {
            e.preventDefault()
            expand()
          }
        }}
      >
        <Alert
          className={`${guideClass} browser-setup-guide--compact`}
          type="success"
          showIcon
          message={
            <span className="browser-setup-guide__compact-row">
              <span>{tf('feishu:browser.setup.networkOk')}</span>
              <ExpandChevronIcon />
            </span>
          }
        />
      </div>
    )
  }

  return (
    <div className={guideClass}>
      <div className="browser-setup-guide__header">
        <Typography.Text strong>{content.title}</Typography.Text>
        {ready ? (
          <Button type="link" size="small" className="browser-setup-guide__collapse" onClick={() => setDetailsExpanded(false)}>
            {t('chat:confirm.collapsible.collapse')}
          </Button>
        ) : null}
      </div>
      {content.summary ? (
        <Typography.Paragraph type="secondary" style={{ marginTop: 8, marginBottom: 8 }}>
          {content.summary}
        </Typography.Paragraph>
      ) : null}

      <div className="browser-setup-guide__status" style={{ fontSize: 13, marginBottom: 12 }}>
        <div>
          Stagehand:{' '}
          {detect.stagehand.installed ? (
            <Typography.Text type="success">{t('status.installed')} {detect.stagehand.version ?? ''}</Typography.Text>
          ) : (
            <Typography.Text type="danger">{t('status.notInstalled')}</Typography.Text>
          )}
        </div>
        <div>
          Playwright:{' '}
          {detect.playwright.installed ? (
            <Typography.Text type="success">{t('status.installed')}</Typography.Text>
          ) : (
            <Typography.Text type="danger">{t('status.notInstalled')}</Typography.Text>
          )}
        </div>
        <div>
          Chromium:{' '}
          {detect.chromium.ready ? (
            <Typography.Text type="success">{t('status.ready')}</Typography.Text>
          ) : (
            <Typography.Text type="danger">{t('status.notInstalled')}</Typography.Text>
          )}
        </div>
        <div>
          Node: {detect.node.version}{' '}
          {detect.node.meetsRequirement ? (
            <Typography.Text type="success">{t('status.builtinNode')} ✓</Typography.Text>
          ) : (
            <Typography.Text type="danger">✗</Typography.Text>
          )}
        </div>
      </div>

      {ready ? (
        <>
          <Alert type="success" showIcon message={tf('feishu:browser.setup.detectPassed')} />
          <Space wrap style={{ marginTop: 12 }}>
            <Button loading={detecting} onClick={() => void onRefresh(true)}>
              {tf('feishu:browser.setup.redetect')}
            </Button>
            <Button onClick={() => void copyText(buildDiagnosticText(detect, platform), tf('feishu:browser.setup.diagnosticInfo'))}>
              {tf('feishu:browser.setup.copyDiagnostic')}
            </Button>
          </Space>
        </>
      ) : (
        <>
          {content.showPackagedDefect ? (
            <Alert type="error" showIcon message={content.summary} style={{ marginBottom: 12 }} />
          ) : (
            <>
              <Typography.Text strong>{tf('feishu:browser.setup.installSteps')}</Typography.Text>
              <ol className="browser-setup-guide__steps" style={{ paddingLeft: 20, marginTop: 8 }}>
                <li>{content.terminalHint}</li>
                <li>
                  {content.cwdLabel}
                  <Space style={{ marginTop: 4 }}>
                    <Typography.Text code>{detect.recommendedCwd}</Typography.Text>
                    <Button size="small" onClick={() => void copyText(detect.recommendedCwd, tf('feishu:browser.setup.copyDir'))}>
                      {tf('feishu:browser.setup.copyDir')}
                    </Button>
                  </Space>
                </li>
                {content.showNpmInstall ? (
                  <li>
                    {tf('feishu:browser.setup.stepInstall')}
                    <Space style={{ marginTop: 4 }}>
                      <Typography.Text code>{content.npmInstallCmd}</Typography.Text>
                      <Button size="small" onClick={() => void copyText(content.npmInstallCmd, tf('feishu:browser.setup.copy'))}>
                        {tf('feishu:browser.setup.copy')}
                      </Button>
                    </Space>
                  </li>
                ) : null}
                {!content.showPackagedDefect ? (
                  <li>
                    {tf('feishu:browser.setup.stepRunInstall')}
                    <Space style={{ marginTop: 4 }}>
                      <Typography.Text code>{content.chromiumInstallCmd}</Typography.Text>
                      <Button size="small" onClick={() => void copyText(content.chromiumInstallCmd, tf('feishu:browser.setup.copyCmd'))}>
                        {tf('feishu:browser.setup.copyCmd')}
                      </Button>
                    </Space>
                  </li>
                ) : null}
                <li>{tf('feishu:browser.setup.stepRedetect')}</li>
              </ol>
            </>
          )}

          <Space wrap style={{ marginTop: 12 }}>
            {typeof window.api?.browserOpenTerminal === 'function' ? (
              <Button
                onClick={() => {
                  void window.api.browserOpenTerminal().then((r) => {
                    if (!r.ok) message.error(formatUserFacingError(r.error))
                  })
                }}
              >
                {tf('feishu:browser.setup.openInTerminal')}
              </Button>
            ) : null}
            <Button loading={detecting} onClick={() => void onRefresh(true)}>
              {tf('feishu:browser.setup.redetect')}
            </Button>
            <Button
              onClick={() => {
                const all = [
                  content.terminalHint,
                  `cd "${detect.recommendedCwd}"`,
                  content.showNpmInstall ? content.npmInstallCmd : '',
                  content.chromiumInstallCmd
                ]
                  .filter(Boolean)
                  .join('\n')
                void copyText(all, tf('feishu:browser.setup.allSteps'))
              }}
            >
              {tf('feishu:browser.setup.copyAllSteps')}
            </Button>
            <Button onClick={() => void copyText(buildDiagnosticText(detect, platform), tf('feishu:browser.setup.diagnosticInfo'))}>
              {tf('feishu:browser.setup.copyDiagnostic')}
            </Button>
          </Space>

          {content.showForceInstall ? (
            <Collapse
              style={{ marginTop: 12 }}
              items={[
                {
                  key: 'force',
                  label: tf('feishu:browser.setup.forceInstallLabel'),
                  children: (
                    <Space>
                      <Typography.Text code>{content.forceInstallCmd}</Typography.Text>
                      <Button size="small" onClick={() => void copyText(content.forceInstallCmd, tf('feishu:browser.setup.copy'))}>
                        {tf('feishu:browser.setup.copy')}
                      </Button>
                    </Space>
                  )
                }
              ]}
            />
          ) : null}

          <Collapse
            style={{ marginTop: 12 }}
            items={[
              {
                key: 'troubleshoot',
                label: tf('feishu:browser.setup.troubleshootLabel'),
                children: (
                  <Space direction="vertical">
                    {content.troubleshooting.map((item) => (
                      <div key={item.title}>
                        <Typography.Text strong>{item.title}</Typography.Text>
                        <div>{item.body}</div>
                      </div>
                    ))}
                  </Space>
                )
              }
            ]}
          />
        </>
      )}
    </div>
  )
}
```

- [ ] **Step 2: 补充缺失的翻译 key**

需要在 `zh-CN/feishu.json` 和 `en-US/feishu.json` 的 `browser.setup` 分支中追加以下 key：

zh-CN:
```json
"clickToExpand": "点击展开详情",
"networkOk": "网络访问功能正常",
"detectPassed": "检测通过，浏览器工具可以初始化。",
"redetect": "重新检测",
"diagnosticInfo": "诊断信息",
"copyDiagnostic": "复制诊断信息",
"installSteps": "安装步骤",
"copyDir": "复制目录",
"copy": "复制",
"copyCmd": "复制命令",
"openInTerminal": "在终端中打开",
"allSteps": "全部步骤",
"copyAllSteps": "复制全部步骤",
"forceInstallLabel": "覆盖损坏安装（进阶）",
"troubleshootLabel": "安装很慢 / 失败？查看故障排除"
```

en-US:
```json
"clickToExpand": "Click to expand details",
"networkOk": "Network access is working",
"detectPassed": "All checks passed. Browser tools can be initialized.",
"redetect": "Re-detect",
"diagnosticInfo": "Diagnostic info",
"copyDiagnostic": "Copy diagnostic info",
"installSteps": "Installation steps",
"copyDir": "Copy directory",
"copy": "Copy",
"copyCmd": "Copy command",
"openInTerminal": "Open in terminal",
"allSteps": "All steps",
"copyAllSteps": "Copy all steps",
"forceInstallLabel": "Overwrite corrupted install (advanced)",
"troubleshootLabel": "Installation slow / failed? View troubleshooting"
```

- [ ] **Step 3: 重新生成类型**

```bash
npm run i18n:generate-types
```

- [ ] **Step 4: 改造 `BrowserSetupGuide.test.tsx`——适配 i18n 上下文**

测试需要用 i18next 初始化。参考现有 Chat 组件测试的 i18n 设置模式。替换 `src/renderer/components/Browser/BrowserSetupGuide.test.tsx`：

```typescript
import { describe, expect, it, vi, beforeEach } from 'vitest'
import { render, screen, fireEvent, waitFor } from '@testing-library/react'
import { App } from 'antd'
import { I18nextProvider } from 'react-i18next'
import i18n from '../../i18n'
import { BrowserSetupGuide } from './BrowserSetupGuide'
import type { BrowserDetectResult } from '../../../shared/browserTypes'

const detectMissing: BrowserDetectResult = {
  stagehand: { installed: true, version: '3.0.0' },
  playwright: { installed: true, browsers: ['chromium'] },
  chromium: { ready: false },
  node: { version: 'v22.0.0', meetsRequirement: true },
  canInitialize: false,
  primaryFailure: 'chromium_missing',
  errors: ['Chromium 未安装'],
  recommendedCwd: 'E:\\Develop\\SpaceAssistant',
  installContext: 'development'
}

const detectReady: BrowserDetectResult = {
  stagehand: { installed: true, version: '3.0.0' },
  playwright: { installed: true, browsers: ['chromium'] },
  chromium: { ready: true },
  node: { version: 'v22.0.0', meetsRequirement: true },
  canInitialize: true,
  primaryFailure: 'ok',
  errors: [],
  recommendedCwd: 'E:\\Develop\\SpaceAssistant',
  installContext: 'development'
}

function renderGuide(detect: BrowserDetectResult | null = detectMissing) {
  const onRefresh = vi.fn().mockResolvedValue(undefined)
  return render(
    <I18nextProvider i18n={i18n}>
      <App>
        <BrowserSetupGuide detect={detect} onRefresh={onRefresh} mode="settings" platform="win32" />
      </App>
    </I18nextProvider>
  )
}

describe('BrowserSetupGuide', () => {
  beforeEach(() => {
    vi.stubGlobal('navigator', {
      ...navigator,
      clipboard: { writeText: vi.fn().mockResolvedValue(undefined) }
    })
    window.api = {
      ...window.api,
      browserOpenTerminal: vi.fn().mockResolvedValue({ ok: true }),
      browserDetect: vi.fn()
    } as typeof window.api
  })

  it('shows install steps when chromium missing', () => {
    renderGuide()
    // 验证 key 已翻译为实际文案（当前 i18n 默认 zh-CN）
    expect(screen.getByText(/浏览器依赖修复/)).toBeTruthy()
    expect(screen.getByText('npx playwright install chromium')).toBeTruthy()
  })

  it('calls onRefresh when re-detect clicked', async () => {
    const onRefresh = vi.fn().mockResolvedValue(undefined)
    render(
      <I18nextProvider i18n={i18n}>
        <App>
          <BrowserSetupGuide detect={detectMissing} onRefresh={onRefresh} mode="settings" platform="win32" />
        </App>
      </I18nextProvider>
    )
    fireEvent.click(screen.getByRole('button', { name: /重新检测/ }))
    await waitFor(() => expect(onRefresh).toHaveBeenCalledWith(true))
  })

  it('collapses to one line when environment is ready', () => {
    renderGuide(detectReady)
    expect(screen.getByText(/网络访问功能正常/)).toBeTruthy()
    expect(screen.getByTitle(/点击展开详情/)).toBeTruthy()
    expect(screen.queryByText(/Stagehand:/)).toBeNull()
  })

  it('expands full guide when compact row clicked', () => {
    renderGuide(detectReady)
    fireEvent.click(screen.getByText(/网络访问功能正常/))
    expect(screen.getByText(/Stagehand:/)).toBeTruthy()
    expect(screen.getByText(/检测通过/)).toBeTruthy()
    expect(screen.getByRole('button', { name: /收起/ })).toBeTruthy()
  })
})
```

- [ ] **Step 5: 运行测试确认通过**

```bash
npx vitest run src/renderer/components/Browser/BrowserSetupGuide.test.tsx
```

预期：4 个测试 PASS。

- [ ] **Step 6: 确保全量测试不受影响**

```bash
npm test
```

预期：913+ 测试全部通过。

- [ ] **Step 7: Commit**

```bash
git add src/renderer/components/Browser/BrowserSetupGuide.tsx src/renderer/components/Browser/BrowserSetupGuide.test.tsx src/renderer/i18n/resources/zh-CN/feishu.json src/renderer/i18n/resources/en-US/feishu.json src/renderer/i18n/types.ts
git commit -m "feat(i18n): migrate BrowserSetupGuide and browserSetupGuideContent to i18n"
```

---

### Task 5: Electron 原生菜单国际化

**Files:**
- Create: `src/shared/menuLabels.ts`
- Modify: `electron/menu.ts`
- Modify: `electron/main.ts`
- Modify: `electron/appIpc.ts`

- [ ] **Step 1: 创建 `src/shared/menuLabels.ts`**

```typescript
export type MenuLocale = 'zh-CN' | 'en-US'

export interface MenuLabels {
  file: string
  view: string
  help: string
  closeWindow: string
  quit: string
  devTools: string
  settings: string
  about: string
  docs: string
}

export const MENU_LABELS: Record<MenuLocale, MenuLabels> = {
  'zh-CN': {
    file: '文件',
    view: '查看',
    help: '帮助',
    closeWindow: '关闭窗口',
    quit: '退出',
    devTools: '开发者工具',
    settings: '设置',
    about: '关于',
    docs: '文档'
  },
  'en-US': {
    file: 'File',
    view: 'View',
    help: 'Help',
    closeWindow: 'Close Window',
    quit: 'Quit',
    devTools: 'Developer Tools',
    settings: 'Settings',
    about: 'About',
    docs: 'Documentation'
  }
}

export function getMenuLabels(locale: string): MenuLabels {
  return MENU_LABELS[locale as MenuLocale] ?? MENU_LABELS['zh-CN']
}
```

- [ ] **Step 2: 改造 `electron/menu.ts`**

```typescript
import { app, Menu, shell } from 'electron'
import { getMainWindow } from './windowRef'
import { getMenuLabels, type MenuLocale } from '../src/shared/menuLabels'

function sendToRenderer(channel: string, ...args: unknown[]): void {
  const w = getMainWindow()
  if (w && !w.isDestroyed()) w.webContents.send(channel, ...args)
}

export function setupAppMenu(locale: string = 'zh-CN'): void {
  const isMac = process.platform === 'darwin'
  const labels = getMenuLabels(locale)

  const fileSubmenu: Electron.MenuItemConstructorOptions[] = []
  if (isMac) {
    fileSubmenu.push({ role: 'close', label: labels.closeWindow })
  } else {
    fileSubmenu.push({
      label: labels.quit,
      accelerator: 'Ctrl+Q',
      click: () => app.quit()
    })
  }

  const template: Electron.MenuItemConstructorOptions[] = []

  if (isMac) {
    template.push({
      label: app.name,
      submenu: [
        { role: 'about' },
        { type: 'separator' },
        { role: 'services' },
        { type: 'separator' },
        { role: 'hide' },
        { role: 'hideOthers' },
        { role: 'unhide' },
        { type: 'separator' },
        { role: 'quit' }
      ]
    })
  }

  template.push({
    label: labels.file,
    submenu: fileSubmenu
  })

  template.push({
    label: labels.view,
    submenu: [
      {
        label: labels.devTools,
        accelerator: isMac ? 'Cmd+Option+I' : 'Ctrl+Shift+I',
        click: () => {
          const w = getMainWindow()
          if (!w) return
          w.webContents.toggleDevTools()
        }
      },
      { type: 'separator' },
      {
        label: labels.settings,
        accelerator: isMac ? 'Cmd+,' : 'Ctrl+,',
        click: () => sendToRenderer('app:open-settings')
      }
    ]
  })

  template.push({
    label: labels.help,
    submenu: [
      {
        label: labels.about,
        click: () => sendToRenderer('app:open-about')
      },
      {
        label: labels.docs,
        click: () => void shell.openExternal('https://github.com/SpaceHuang/SpaceAssistant')
      }
    ]
  })

  if (isMac) {
    template.push({ role: 'windowMenu' })
  }

  Menu.setApplicationMenu(Menu.buildFromTemplate(template))
}

export function rebuildAppMenu(locale: string): void {
  setupAppMenu(locale)
}
```

- [ ] **Step 3: 改造 `electron/main.ts`——传入 locale**

在 `electron/main.ts` 中找到 `setupAppMenu()` 调用行，改为从配置读取 locale：

```typescript
// 在 setupAppMenu() 调用处，改为：
const locale = db ? readAppLocale(db) : 'zh-CN'
setupAppMenu(locale)
```

需要从 `appIpc.ts` 导入或复用 `readAppLocale`。如果 `readAppLocale` 在 `appIpc.ts` 中定义且未导出，需将其提取为独立函数或导出。

实际上 `electron/main.ts:328` 调用 `setupAppMenu()` 时 db 可能尚未初始化。改为在 `appIpc.ts` 的 `config:set` handler 中重建菜单：

在 `electron/appIpc.ts` 的 `config:set` handler 中，locale 变更时添加菜单重建：

```typescript
// 在 electron/appIpc.ts 顶部添加 import
import { rebuildAppMenu } from './menu'

// 在 config:set handler 中（约 line 636），locale 保存后添加：
if (payload.locale !== undefined && isAppLocale(payload.locale)) {
  setConfigValue(ctx.db, CONFIG_KEYS.locale, payload.locale)
  rebuildAppMenu(payload.locale)
}
```

同时，在 `electron/main.ts` 中，`setupAppMenu()` 调用前从数据库读取 locale：

```typescript
// electron/main.ts 约 line 328
// 改造前：
setupAppMenu()

// 改造后：
import { readAppLocale } from './appIpc'  // 如果 readAppLocale 已导出
// 或直接在 main.ts 中读取配置
```

由于 `readAppLocale` 是 `appIpc.ts` 内部函数，需要导出它：

在 `electron/appIpc.ts` 中将 `readAppLocale` 改为 `export`：

```typescript
export function readAppLocale(db: AppDatabase): AppConfig['locale'] {
  // ... 现有实现
}
```

然后在 `electron/main.ts` 中：

```typescript
import { setupAppMenu } from './menu'
import { readAppLocale } from './appIpc'

// 在 app.whenReady() 中，db 初始化后：
const locale = readAppLocale(db)
setupAppMenu(locale)
```

- [ ] **Step 4: 验证菜单在语言切换时更新**

在渲染进程中切换语言后，确认：
1. `persistLocaleToBackend` 调用 `window.api.configSet({ locale })`
2. `config:set` handler 调用 `rebuildAppMenu(locale)`
3. 菜单标签即时更新

- [ ] **Step 5: Commit**

```bash
git add src/shared/menuLabels.ts electron/menu.ts electron/main.ts electron/appIpc.ts
git commit -m "feat(i18n): localize Electron native menu with locale-aware labels"
```

---

### Task 6: `i18n:check` strict 模式 + 审计报告

**Files:**
- Modify: `scripts/i18n-check.ts`
- Modify: `package.json`

- [ ] **Step 1: 增强 `scripts/i18n-check.ts`——分类统计**

在 `checkHardcodedChinese` 函数中增加分类计数：

```typescript
function checkHardcodedChinese(): boolean {
  const srcDir = path.resolve(__dirname, '../src/renderer')
  const files = walkDir(srcDir, ['.tsx', '.ts'])
  const chinesePattern = /[一-鿿]/
  let totalCount = 0
  let testCount = 0
  let codeCount = 0

  for (const file of files) {
    if (file.includes(`${path.sep}i18n${path.sep}resources${path.sep}`)) continue
    const content = fs.readFileSync(file, 'utf-8')
    const lines = content.split('\n')
    const isTestFile = file.includes('.test.') || file.includes(`${path.sep}test${path.sep}`)
    for (let i = 0; i < lines.length; i++) {
      if (chinesePattern.test(lines[i]!)) {
        const relPath = path.relative(path.resolve(__dirname, '..'), file)
        if (strictHardcoded) {
          console.warn(`⚠️  Hardcoded Chinese: ${relPath}:${i + 1}`)
        }
        totalCount++
        if (isTestFile) {
          testCount++
        } else {
          codeCount++
        }
      }
    }
  }

  console.log(`${totalCount} hardcoded Chinese occurrences found (${codeCount} in source, ${testCount} in tests)`)
  if (strictHardcoded) {
    if (codeCount > 0) {
      console.error(`❌ ${codeCount} hardcoded Chinese in source files`)
      return false
    }
    if (testCount > 0) {
      console.warn(`⚠️  ${testCount} hardcoded Chinese in test files (allowed for now)`)
    }
  }
  return true
}
```

- [ ] **Step 2: 在 `package.json` 新增 strict script**

```json
"i18n:check:strict": "tsx scripts/i18n-check.ts --strict-hardcoded"
```

- [ ] **Step 3: 运行验证**

```bash
npm run i18n:check          # 现有 warn 模式，确认仍通过
npm run i18n:check:strict   # strict 模式，查看 source vs test 分类
```

- [ ] **Step 4: 输出审计报告**

```bash
npm run i18n:check 2>&1 | grep "⚠️" | grep -v ".test." > i18n-audit-source.txt
npm run i18n:check 2>&1 | grep "⚠️" | grep ".test." > i18n-audit-tests.txt
```

确认 source 分类中的硬编码均为可接受类别（如 `browserSetupGuideContent.ts` 已在 Task 3 中处理完毕）。

- [ ] **Step 5: Commit**

```bash
git add scripts/i18n-check.ts package.json
git commit -m "feat(i18n): add --strict-hardcoded mode with source/test classification"
```

---

### Task 7: 英文校对

**Files:**
- Modify: 8 个 `src/renderer/i18n/resources/en-US/*.json`

- [ ] **Step 1: 逐文件校对 en-US JSON**

校对检查项：
- 术语一致性（Session、Tool Call、Feishu 等与术语表对齐）
- 语法正确性
- 自然度（避免机翻感）

**`en-US/common.json`：** 已校对（见 Task 2 Step 3 新增 `status.*`，其他 key 在第二期已完成）

**`en-US/chat.json`：** 重点检查：
- `tool.labels.*` 工具标签是否自然
- `confirm.*` 确认卡文案是否一致
- `chatView.empty.*` 空态文案是否友好

**`en-US/config.json`：** 最大文件（313 行），逐 section 检查：
- `models.*` 模型设置相关
- `llmService.*` LLM 服务管理
- `skills.*` Skill 管理
- `tools.*` / `shell.*` / `browser.*` 工具设置
- `feishu.*` 飞书设置（已有，第二期完成）
- `wiki.*` Wiki 设置

**`en-US/errors.json`：** 16 个错误码文案，确认语气一致、信息充分。

**`en-US/fileTree.json`、`en-US/search.json`、`en-US/feishu.json`、`en-US/wiki.json`：** 第三期新增，逐 key 检查。

- [ ] **Step 2: 修正发现的问题**

在校对过程中发现的任何不自然/错误翻译，直接修正对应 JSON 文件中的值。

- [ ] **Step 3: 验证 key 对齐未被破坏**

```bash
npm run i18n:check
```

预期：✅ 通过。

- [ ] **Step 4: Commit**

```bash
git add src/renderer/i18n/resources/en-US/
git commit -m "fix(i18n): proofread and polish English translations across all namespaces"
```

---

### Task 8: CI 接入 + 文档更新

**Files:**
- Modify/Create: `.github/workflows/ci.yml`（或现有 CI 配置）
- Modify: `CLAUDE.md`

- [ ] **Step 1: 检查现有 CI 配置**

```bash
ls .github/workflows/ 2>/dev/null || echo "No workflows directory"
```

如果已有 CI 配置，在 test 步骤后添加 i18n check。如果没有，创建最小 CI 配置：

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      - run: npm ci
      - run: npm run i18n:generate-types
      - run: npm run i18n:check
      - run: npm test
```

- [ ] **Step 2: 更新 `CLAUDE.md`**

在 CLAUDE.md 中补充 i18n 开发规范。在「UI 规范」section 之后新增：

```markdown
## i18n 国际化开发规范

- 所有新增 UI 文案**必须**通过 `t()` 函数使用，禁止硬编码中文或英文
- 翻译 key 命名遵循 `模块.组件.语义` 层级（camelCase，最多 4 层）
- `zh-CN` 是翻译 key 的真实来源；TypeScript 类型从 `src/renderer/i18n/resources/zh-CN/` 自动推导
- 新增 key 后运行 `npm run i18n:generate-types` 更新类型
- 提交前运行 `npm run i18n:check` 确保 key 对齐、JSON 合法
- 复用性高的文案放 `common` 命名空间；模块专属文案放对应命名空间
- 错误消息使用 `errors` 命名空间 + `src/shared/errorCodes.ts` 错误码模式
- 翻译资源文件在 `src/renderer/i18n/resources/{locale}/` 下按命名空间分文件
```

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/ CLAUDE.md
git commit -m "chore(i18n): add CI i18n check and update CLAUDE.md with i18n conventions"
```

---

### Task 9: 最终全量验证

- [ ] **Step 1: 运行全量测试**

```bash
npm test
```

预期：全部测试通过（913+ 用例，含本计划新增/修改的测试）。

- [ ] **Step 2: 运行 i18n check**

```bash
npm run i18n:check
```

预期：✅ i18n check passed。确认 source 分类硬编码数较第四期前下降（移除了 `browserSetupGuideContent.ts` 和 `BrowserSetupGuide.tsx` 的硬编码）。

- [ ] **Step 3: 生成类型并确认无 TypeScript 错误**

```bash
npm run i18n:generate-types
npx tsc --noEmit -p tsconfig.json 2>&1 | head -20
```

- [ ] **Step 4: 构建验证**

```bash
npm run build
```

预期：构建成功。

- [ ] **Step 5: 更新需求文档进度**

更新 `docs/requirement/i18n-architecture-requirement.md` 中 §1.3 的进度状态，将第四期标记为已完成。

- [ ] **Step 6: 最终 Commit**

```bash
git add docs/requirement/i18n-architecture-requirement.md
git commit -m "docs(i18n): mark phase 4 complete in requirement document"
```

---

## 实施顺序总结

```
Task 1: formatChatTimestamp 国际化
  ↓
Task 2: 新增翻译 key（browser.setup + common.status）
  ↓
Task 3: browserSetupGuideContent.ts 工厂化
  ↓
Task 4: BrowserSetupGuide.tsx 组件 i18n 迁移
  ↓
Task 5: Electron 原生菜单国际化
  ↓
Task 6: i18n:check strict 模式
Task 7: 英文校对（与 Task 6 可并行）
  ↓
Task 8: CI 接入 + 文档更新
  ↓
Task 9: 最终全量验证
```
