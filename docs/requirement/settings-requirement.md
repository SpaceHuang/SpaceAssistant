# 设置界面需求文档

## 1. 设置界面整体结构

设置界面采用 Modal 弹窗 + Tab 分页的形式，宽度 560px，包含两个 Tab 页：

| Tab | 名称 | 包含内容 |
|-----|------|---------|
| 通用 | 通用 | 工作目录 |
| 大模型 | 大模型 | API Key、Base URL、模型列表、Temperature、默认开启 Thinking |

### 1.1 界面样式

- Modal body 内表单标签字体统一为 13px
- 模型列表标题行右侧放置操作按钮，与表单控件右边缘对齐

---

## 2. 通用 Tab

### 2.1 工作目录

| 需求点 | 描述 |
|--------|------|
| 输入框 | 显示当前工作目录路径，支持手动输入 |
| 目录选择 | 输入框右侧提供按钮，点击后弹出系统文件夹选择器，支持新建文件夹 |
| 写入权限校验 | 用户设置完工作目录后，主动检查应用是否有写入权限；若无权限，在输入框下方显示红色错误提示，说明该目录不可用（无法写入），需更换 |
| 校验时机 | (1) 通过选择器选择目录后立即校验；(2) 手动输入路径后失焦时校验；(3) 保存时再次校验，不可写则阻止保存 |
| 必填 | 工作目录为必填项 |

---

## 3. 大模型 Tab

### 3.1 API Key

| 需求点 | 描述 |
|--------|------|
| 输入类型 | 密码输入框，内容隐藏 |
| 占位提示 | sk-ant-... |
| 留空逻辑 | 留空表示不修改现有 Key |
| 必填 | 否 |

### 3.2 Base URL

| 需求点 | 描述 |
|--------|------|
| 输入类型 | 文本输入框 |
| 占位提示 | 默认 Anthropic 官方 |
| 必填 | 否，为空时使用官方默认地址 |

### 3.3 模型列表

模型列表替代了原来的单个"模型"和"默认模型"文本输入，改为可视化的列表管理界面。

#### 3.3.1 模型数据结构

每个模型条目包含以下字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| name | string | 模型名称（不可重复） |
| maximumContext | number | 最大上下文长度 |
| maxTokens | number | 最大输出长度 |
| isDefault | boolean | 是否为默认模型（列表中只能有一个默认，且列表非空时必须有一个） |
| isFast | boolean | 是否为快速模型 |
| enabled | boolean | 是否启用（禁用后该模型不可用于聊天，半透明显示） |

#### 3.3.2 内置默认模型列表

首次使用时预置以下模型（数据来源：`res/resource/modes.md`）：

| 模型名称 | maximumContext | maxTokens | isDefault | isFast | enabled |
|---------|---------------|-----------|-----------|--------|---------|
| kimi-k2.6 | 262144 | 98304 | 否 | 否 | 是 |
| glm-5.1 | 200000 | 128000 | 是 | 否 | 是 |
| minimax-m2.7 | 204800 | 204800 | 否 | 否 | 是 |
| deepseek-v4-pro | 1000000 | 384000 | 否 | 否 | 是 |
| deepseek-v4-flash | 1000000 | 384000 | 否 | 是 | 是 |
| claude-sonnet-4-6 | 1000000 | 64000 | 否 | 否 | 是 |
| claude-opus-4-7 | 1000000 | 128000 | 否 | 否 | 是 |
| claude-haiku-4-5 | 200000 | 64000 | 否 | 是 | 是 |
| gpt-5.5 | 1000000 | 128000 | 否 | 否 | 是 |
| gemini-3.1-pro | 1000000 | 65536 | 否 | 否 | 是 |
| gemini-3.1-flash-lite | 1000000 | 64000 | 否 | 是 | 是 |

#### 3.3.3 列表显示

- 每个模型占两行显示：
  - **第一行**：启用开关（Switch）+ 模型名称 + "默认"标签（蓝色）+ "快速"标签（橙色）+ 删除按钮
  - **第二行**（缩进）：上下文长度 + 输出长度 + "默认"复选框 + "快速"复选框
- 禁用的模型整行半透明（opacity: 0.45），复选框不可操作
- 大数字友好显示：1000000 → 1M，262144 → 262.1K，8192 → 8.2K

#### 3.3.4 操作按钮

位于"模型列表"标题行的右侧，与表单控件右边缘对齐：

| 按钮 | 图标 | 行为 |
|------|------|------|
| 恢复默认 | mingcute refresh_2_line | 将模型列表重置为内置默认列表 |
| 添加模型 | mingcute add_line | 点击弹出 Popover 浮层，供用户输入新模型信息 |

#### 3.3.5 添加模型 Popover

包含以下输入项：

| 字段 | 控件 | 说明 |
|------|------|------|
| 模型名称 | Input | 必填，不可与已有模型重名 |
| 最大上下文 | InputNumber | 默认值 200000 |
| 最大输出 | InputNumber | 默认值 8192 |
| 默认 | Checkbox | 勾选后，原有默认模型自动取消默认 |
| 快速 | Checkbox | 标记为快速模型 |
| 确认 | Button | 验证通过后添加到列表 |

#### 3.3.6 默认模型逻辑

- 列表中只能有一个默认模型
- 列表非空时，必须有一个默认模型
- 勾选某个模型为默认时，自动取消其他模型的默认
- 删除默认模型后，自动将列表中第一个模型设为默认
- 禁用默认模型时，自动将默认角色转移给下一个启用的模型
- 保存时校验：至少需启用一个模型

### 3.4 Temperature

| 需求点 | 描述 |
|--------|------|
| 控件 | InputNumber |
| 范围 | 0 ~ 2 |
| 步长 | 0.1 |

### 3.5 默认开启 Thinking

| 需求点 | 描述 |
|--------|------|
| 控件 | Switch |
| 默认值 | 开启（true） |
| 说明 | 控制新建会话时是否默认启用模型的 Thinking 功能 |

---

## 4. 底部操作栏

| 按钮 | 行为 |
|------|------|
| 取消 | 关闭设置弹窗，不保存 |
| 测试连接 | 使用当前配置向 API 发送测试请求，验证连通性 |
| 保存 | 校验所有字段后保存配置 |

---

## 5. 数据存储与兼容性

### 5.1 存储方式

- 模型列表以 JSON 字符串存储在 `config.models` 键中
- 保存模型列表时，自动从默认模型条目同步 `config.model` 和 `config.defaultModel` 字段（向后兼容）
- `thinkingEnabled` 存储在 `config.thinkingEnabled` 键中

### 5.2 迁移策略

- 首次加载时若 `config.models` 为空，从旧的 `config.model` / `config.defaultModel` 字段或使用内置默认列表自动生成
- `AppConfig` 类型中保留 `model` / `defaultModel` 字段，供 `ChatView` 等模块直接使用

---

**文档版本**: v1.0
**创建日期**: 2026年5月15日
**适用范围**: SpaceAssistant 设置界面
